# preview-db-init/

**Purpose:** Postgres init scripts for the preview-stack Docker images.

**Belongs here:** SQL/shell init scripts for the preview environment only.

**Never here:** Production data, production credentials, or anything run against production (CLAUDE.md line 239 ethos; CLAUDE.md line 645).

**CLAUDE.md rule:** CLAUDE.md line 77.

**Status:** Foundation only — empty until a Docker preview stack with Postgres is actually set up.

**Verification:** Preview stack boots and the init scripts run without manual intervention (once they exist).

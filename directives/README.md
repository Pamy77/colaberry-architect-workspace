# directives/

**Purpose:** SOPs and runbooks — what to build and how success is verified, written before code.

**Belongs here:** Human-readable `.md` files defining goals, inputs, outputs, edge cases, safety constraints, and verification expectations.

**Never here:** Business logic, executable code, secrets (CLAUDE.md line 79).

**CLAUDE.md rule:** Layer 1 of the Architecture model (CLAUDE.md lines 40, 69).

**Status:** NOW — first folder populated, since it costs nothing and everything else depends on it.

**Verification:** Directive validated for required sections, referenced files/scripts actually existing, and markdown integrity (CLAUDE.md lines 250-252).

See `00-project-setup.md` in this folder for the current onboarding directive.
